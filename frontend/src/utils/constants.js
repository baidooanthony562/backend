// Local fallback for the low-stock threshold. The dashboard prefers the value
// the backend returns (stats.lowStockThreshold) so the two stay in sync; this
// is only used until that value is available.
export const LOW_STOCK_THRESHOLD = 5;
